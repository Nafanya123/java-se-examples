-- Drop table

-- DROP TABLE public.users

	CREATE TABLE users (user_id varchar(100) NOT NULL,
                        first_name varchar(20) NOT NULL,
                        last_name varchar(20) NOT NULL,
                        age number(4) NULL)
);
